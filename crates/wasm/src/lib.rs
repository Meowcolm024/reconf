//! Placeholder crate for target-specific WebAssembly bindings.
//!
//! The native compiler and CLI refactor keeps the reusable pipeline in
//! `reconf-compiler`. The WASM branch can add bindings here without adding
//! target-specific code to core language crates.
